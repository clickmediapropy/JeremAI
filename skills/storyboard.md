# Skill: storyboard

## Do
1. `jeremai search-broll --client <id> --query "<shot>"` **before** any generate.
2. Prefer claim-safe library clips (Drive vault, R2 cache, local index).
3. Only storyboard a generated shot when search misses.
4. Keep product fidelity shots on real pack footage when the hash matches the live SKU.

## Shot list (Aether demo)
| t | shot | source |
| --- | --- | --- |
| 0–3s | hook / kitchen window | library |
| 3–7s | scoop + water | library or gen |
| 7–12s | walk out the door | library |
| 12–15s | product + disclaimer super | editor |

Paid-test metric: **≥40% of final seconds from reused library**.
