# Comfy / Ref2VA workflows

Drop API-format Comfy graphs here (`*.json`). Keep them client-agnostic.

Client brand + claims come from `knowledge/clients/<id>/` at run time. See `skills/comfyui.md`.

This MVP does not execute graphs. `jeremai assemble` still consumes mp4s from `.data/clips/` or the asset index.
